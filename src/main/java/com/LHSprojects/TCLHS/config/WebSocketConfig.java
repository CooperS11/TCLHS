package com.LHSprojects.TCLHS.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebSocketConfig {
    // WebSocket/STOMP removed — application now uses HTTP REST endpoints instead.
}
